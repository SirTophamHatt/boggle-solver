"use strict";
exports.__esModule = true;
var chai_1 = require("chai");
var test_helper_1 = require("./test-helper");
var histogram_1 = require("../histogram");
suite("Histogram", function () {
    suite("Samples", function () {
        var tests = [
            [[], []],
            [['a', 'b', 'c'], [['a', 1], ['b', 1], ['c', 1]]],
            [['a', 'b', 'b'], [['b', 2], ['a', 1]]]
        ];
        test_helper_1.dataDrivenTest(tests, function (data, expect) {
            var hist = new histogram_1.Histogram();
            for (var j = 0; j < data.length; j++) {
                hist.add(data[j]);
            }
            chai_1.assert.deepEqual(hist.highest(), expect);
        });
    });
    test("countOf", function () {
        var hist = new histogram_1.Histogram();
        chai_1.assert.equal(hist.countOf(1), 0);
        hist.add(1);
        chai_1.assert.equal(hist.countOf(1), 1);
        hist.add(2);
        chai_1.assert.equal(hist.countOf(2), 1);
    });
});
//# sourceMappingURL=histogram-test.js.map