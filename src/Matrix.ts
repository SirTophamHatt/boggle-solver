export type Matrix<T> = T[][];

/*
export function createMatrix(rows: number, columns: number, values?: Matrix): Matrix {
    return Array.from(new Array(rows), (_, row) => {
        return Array.from(new Array(columns), (_, column) => {
            return values[row][column];
        });
    });
}*/
